# SQL-lib

SQL().asStream(size?)
SQL().asAsyncStream(size?, ec)
SQL().asObservable(size?, ec?)

SQL().headOption
SQL().reduce
SQL().groupBy
SQL().indexBy
SQL().foreach

SQL().single(map?)
SQL().all(map?)


batch()
